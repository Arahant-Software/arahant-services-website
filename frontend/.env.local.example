# URL of the backend API (see /backend) that powers the "Get a Call Back" form.
# Copy this file to .env.local and fill in the value below.
# .env.local is gitignored by default in Next.js and must never be committed.

NEXT_PUBLIC_API_URL=http://localhost:5000
